# 专项修复进度与验收范围

更新于 2026-09-23。已保留先前的 `037c4402`（容器与图片关系）、`7e33ac49`（DOCX 图表与 Worker）、`23187313`（旧 Excel 文本解码）三批修复；新增 `c28d5cda` 的 LRC 预览、`fa0ad15c` 的表格尺寸状态修复及 `7dbf9b0e` 的 OFD 适配精度修复。本轮再增加 `68b7d4ee` 的 PDF 隐藏视口适配修复和 `088a0dcb` 的 PPTX 编号/段距继承修复；`a3c913be` 提交六份 PDF 原样验收。具体结果与回归边界见各专项记录。

## 专项状态

| 专项 | 已确认结果 | 剩余范围 |
| --- | --- | --- |
| W01 Word 内容识别与旧格式 | C061 文本、C068 UTF-16 HTML 及 C009 内文本型 DOC 已走正确路径；OLE、OOXML、WordML 保持独立识别 | 11 MB 二进制 DOC 及原生 DOC 图片、表格的完整版式 |
| W02 Word 版式、表格、印章 | 保留既有浮动修复；**C022 异常段距、重复分页布局与正文完整性子项已修复验收**。30,769 个原始正文字符、87 行表格和逐页页脚完整；固定校正基线与优化组逐页、逐元素几何一致；实际冻结锁安装和 Worker 检查通过 | 全部页脚定位、制表符、编号、文字方向、印章及其余原稿逐页版式；不将当前字体下页数当作桌面应用参考，不承诺统一性能指标 |
| W03 Word/Excel 图表 | C038 两图各 16 个稀疏标签；C012 的 13 个标签；C052 分类轴；真实 Worker 返回 parsed | Excel 原稿图表、全部窄容器/DPR 组合及整篇文档保真 |
| P01 PPTX 图层与排版 | 保留图片/页数及 C050 编号/段距验收；**混合文字域顺序、动态显示页码、连续换行、图片透明度与局部翻转、母版/布局堆叠子项已修复**。C010/C008 原样图片属性与 XML 独立对照通过；4 份原稿可见页码域复测；33 项专项检查和 12 份/60 页解析回归通过 | 其余字体、对齐、日期框定位、长文本度量及复杂图表/渐变/掩码的逐元素验收；不把生成式通过扩大成整页像素保真 |
| O01 OFD 文字、印章与页序 | **已验收所给 6 份原稿的页序、文字坐标、推进、CTM、前导空白、中点及图片加载子项**：17 页在两档宽度下复测，997 个 TextCode、34 个中点可见像素；页面比例取整溢出已修复 | 全文字体逐像素对齐不作通过判定；印章可见不代表签名有效性，也不扩大到未覆盖的签章类型 |
| F01 PDF 尺寸、图像与加载 | **隐藏弹窗 initial-fit 已修复；6 份原件 63 页在 DPR 1/2 下 126 次核对通过**。原始比例、位图分辨率、单内容滚动容器、实际 Worker、本地资源和受控同源 Referer/403 已验收；C047 确认为原稿合法空白，不计作修复 | 客户真实服务端策略、iOS Safari/企业 WebView、桌面阅读器逐像素等价；扫描件不以算法虚构清晰度。受控 HTTP 验证不替代目标宿主 |
| S01 表格交互与原始样式 | **行列拖拽、缓存切表、缩放与延迟回填覆盖子项已修复验收**；C036 原样拖拽及 50%/88%/100%/200% 缩放通过，双工作表切换由独立生成样本覆盖 | 原稿图表、图片位置、全部字体和整表像素保真 |
| A01 压缩包文件名 | **C009 的 GBK 中文/圈号文件名、列表搜索及三个子文件正文预览已原样验收**；提取内容与独立读取的存储块逐字节一致，下载内容哈希一致 | 本地内存页面的浏览器建议下载名返回默认值，下载属性本身正确；真实目标宿主保存名仍需验收。其他代码页、加密/RAR 不从本组结论外推 |
| T01 LRC 预览能力 | **静态 LRC 预览已结项**：时间标注、纯歌词、原文视图、逐行/逐词、多标签、偏移、编码及 C067 原样验收已有记录 | 不包含音频播放或外部音轨同步；不是全部文本格式验收 |
| R01 打印纸张与页边距 | 混排纸张、原始旋转、Canvas 显示盒与资源就绪已有回归记录 | 原稿全部内容、真实打印驱动覆盖与目标宿主 |

## 本轮新增证据

[PDF 视口与原样验收](pdf-viewport-20260923.md)：9 项真实浏览器/Worker 生命周期检查，16 项原样与受控请求策略检查；运行 `35817304468`、`35817787925`，共 126 次原样页面核对。

[PPTX 编号与段距](pptx-paragraph-20260923.md)：11 项生成式/行为检查和 C050 原样检查共 12 项通过；运行 `35818968969`，旧实现定向失败、修复后通过。

[冻结锁实际安装复验](evidence/pdf-pptx-frozen-20260923/verification.json)：运行 `35819650869` 使用 pnpm 11.0.9 实际安装根工具链及 Core、PDF、PPTX 相关工作区包，保留已声明补丁，冻结锁未变化；三包构建、PDF 9 项、PPTX 11 项以及两套既有 PPTX 回归通过。不是 lockfile-only，但也不是全工作区发版构建。对应提交 `16a29ec3` 同时修正测试工具依赖声明和包内解析路径。

以下为前批已取得的证据，不重复计作新增修复。

## 先前证据

[表格状态修复](spreadsheet-layout-20260923.md)：本地 11 项（含原样）通过；远端 10 项鼠标交互通过，Core/Spreadsheet 构建与既有异常维度、富文本回归通过。

[OFD 布局复测](ofd-layout-20260923.md)：本地 6 份哈希匹配原稿、17 页、两档宽度/DPR 2；远端 25 组适配几何检查及六套既有回归，Core/OFD 构建通过。

[压缩包原样验收](archive-original-20260923.md)：实际 UI、预设驱动子预览、提取、搜索、下载字节及清理 8 项通过；浏览器保存名未计为通过项。

[LRC 静态预览](lrc-20260923.md) 与 [ZIP 文件名修复](archive-filenames-20260923.md) 是上批已推送内容，不重复计作本次新修复。原文件不会因验收而修改；测试报告使用中性编号与哈希。

## 清单边界

原清单 109 条记录的分流为 47 条原样复测候选、23 条缺原文件或完整复现、8 条能力与交互选择、25 条已有同类能力或防护、5 条不同依赖链或范围限制、1 条已有同字节回归。分流数不是修复数，专项中的子项也不能相加充当独立问题总数。

**资料包整体尚未结项。** 本轮增加 PDF 桌面原样与请求策略、PPTX 目录与段距的范围结项；Word/PPTX 等未完成的逐页保真、其他大文档性能及目标宿主问题仍保持开放。没有批量关闭 issue，没有创建 PR、合并 main 或发布 npm 包。

先前组合验证记录位于 `evidence/corpus-combined-20260922/`。它的冻结锁检查仅是 lockfile-only，不能称为全工作区发版验收。本次专项构建和测试也没有取代最终冻结锁全量构建。

DOCX 修复仍依赖仓库 `patchedDependencies`，安装时不得跳过；单独安装 npm 的旧版本不会自动得到审阅分支的引擎修复。

## C022 分页专项补充

功能提交 `e9ff0a50` 已通过原样完整性与固定对照验收，详见 [段距与分页记录](docx-paragraph-flow-20260923.md) 和 `evidence/docx-paragraph-flow-20260923/original-verification.json`。重复测量按相反顺序运行，报告仅保留数值和哈希。新增检查验证分支当前的真实方法，不替换已有修复或用副本方法代替产品；其他未结专项状态不变。

## PPTX 绘制语义补充

[混合文本域、图片效果与母版层次](pptx-drawing-semantics-20260923.md) 记录本轮通用根因、原样子项和范围边界。生产 Worker 与演示站静态 Worker 已同步并按同字节验证；独立部署需要更新已复制的 Worker。其余专项不因 PPTX 检查通过而关闭。
